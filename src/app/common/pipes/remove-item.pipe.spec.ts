import { RemoveItemPipe } from './remove-item.pipe';

describe('RemoveItemPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveItemPipe();
    expect(pipe).toBeTruthy();
  });
});
